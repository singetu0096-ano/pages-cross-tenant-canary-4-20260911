# GitHub Pages owned-account isolation canary

This public repository is used only for a bounded GitHub Pages isolation test
between two accounts owned by the same researcher. The normal deployment
contains a non-sensitive random marker and no application code.

